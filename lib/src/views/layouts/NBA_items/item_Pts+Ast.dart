import 'package:flutter/material.dart';

class ItemPstAst extends StatelessWidget {
  const ItemPstAst({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blueAccent,
      ),
    );
  }
}
