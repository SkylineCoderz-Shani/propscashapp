import 'package:flutter/material.dart';

class ItemRebounds extends StatelessWidget {
  const ItemRebounds({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.brown,
      ),
    );
  }
}
