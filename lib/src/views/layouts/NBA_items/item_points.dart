import 'package:flutter/material.dart';

class ItemPoints extends StatelessWidget {
  const ItemPoints({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
