import 'package:flutter/material.dart';

class ItemPstRebAst extends StatelessWidget {
  const ItemPstRebAst({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.yellow,
      ),
    );
  }
}
