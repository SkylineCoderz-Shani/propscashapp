import 'package:flutter/material.dart';

class ItemSteals extends StatelessWidget {
  const ItemSteals({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.orange,
      ),
    );
  }
}
