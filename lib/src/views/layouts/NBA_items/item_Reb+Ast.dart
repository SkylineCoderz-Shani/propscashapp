import 'package:flutter/material.dart';

class ItemRebAst extends StatelessWidget {
  const ItemRebAst({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.yellow,
      ),
    );
  }
}
