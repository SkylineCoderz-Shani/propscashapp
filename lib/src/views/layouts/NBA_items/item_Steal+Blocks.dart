import 'package:flutter/material.dart';

class ItemStlBlk extends StatelessWidget {
  const ItemStlBlk({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.green,
      ),
    );
  }
}
