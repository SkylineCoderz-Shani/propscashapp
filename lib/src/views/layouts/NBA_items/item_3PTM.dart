import 'package:flutter/material.dart';

class Item3PTM extends StatelessWidget {
  const Item3PTM({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.blue,
      ),
    );
  }
}
