import 'package:flutter/material.dart';

class ItemBlocks extends StatelessWidget {
  const ItemBlocks({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
      ),
    );
  }
}
