import 'package:flutter/material.dart';

class ItemTurnovers extends StatelessWidget {
  const ItemTurnovers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.yellow,
      ),
    );
  }
}
